# Netlify 免费部署配置（GitHub 登录，无需手机验证）
[build]
  # 使用 npm（Netlify 的 Next.js 运行时对 npm 兼容最稳定）
  command = "npm run build"

[build.environment]
  # 固定与 Netlify 构建环境一致的具体版本，避免缓存键复用旧构建产物
  NODE_VERSION = "22.23.2"
